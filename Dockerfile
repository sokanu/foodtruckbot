FROM node:10-alpine

ARG CLIENT_SECRET
ARG CLIENT_ID

WORKDIR /app

COPY . /app
COPY ./package*.json /app/

RUN npm install

EXPOSE 8080

ENV CLIENT_ID ${CLIENT_ID}
ENV CLIENT_SECRET ${CLIENT_SECRET}
ENV PORT 8080

CMD ["npm", "start"]